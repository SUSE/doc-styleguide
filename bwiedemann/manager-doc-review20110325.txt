reviewed http://docserv.suse.de/generated/books/Manager/susemanager-proxy-quick_remarks_comments_draft/

?in the customer environment. # customer's ?

?create a backup of your hard disks. # wird doch nur eine benutzt - oder ist das für den Fall, dass man die falsche HDD auswählt?

?register SUSE Manager Proxy with SUSE Manager (see Section 3.2.1, “Registering with SUSE Manager” # is a forward ref to next line?

?that corresponds to their installed version of SUSE Linux Enterprise. # funktioniert das nicht mit RHEL?

?you will unset the currently used universal key by activating the checkbox. # will=>have|need to?

channels to which to subscribe to # ein to zu viel

?Proceed with Procedure 17, “Editing the Bootstrap Script and Registering Proxy Systems”. # is a forward ref to next line

?To be on the save side, check ... # was sollte man dort sehen?

?activates services such as squid, apache2, and jabberd, required by SUSE Manager Proxy. # wordorder?


=====
http://docserv.suse.de/generated/books/Manager/susemanager-client-config_remarks_comments_draft/preface.susemanager.clientconf.html
http://docserv.suse.de/generated/books/Manager/susemanager-client-config_remarks_comments_draft/ch-intro.html
# no maintainer?

http://docserv.suse.de/generated/books/Manager/susemanager-client-config_remarks_comments_draft/ch-latest-clients.html

deploying yum, pup , and rhn_register (or up2date ) for the first time # spaces, layout
yum, pup , and rhn_register (or up2date ) # spaces
the Red Hat Update Agent (up2date ) and possibly the Red Hat Network Registration Client (rhn_register ) # spaces

?By default, the rhn_register and up2date refer to the main SUSE Manager Servers. Users must reconfigure client systems to refer to their SUSE Manager Server or SUSE Manager Proxy Server. # das "main SUSE Manager Servers" könnte da verkehrt sein

?looks something like this: # ist das gutes Englisch?
your-suse_manager-FQDN # bei den Kommandos (kursiv/placeholder) die zu ersetzenden Teile hervorheben (wie in Documentation Conventions angegeben) # evtl. -FQDN durch .example.com ersetzen oder FQDN etwas früher erklären?

You may list the central SUSE Manager Servers, as well. # wie oben

?Ensure that your SUSE Manager Server is version 1.2 or later # ist immer gegeben => streichen?

http://docserv.suse.de/generated/books/Manager/susemanager-client-config_remarks_comments_draft/ch-certificates.html

?installing SUSE Manager within their infrastructures # infrastructure?

?allow you to build your own SSL keys and certificates based on your own private Certificate Authority (CA) during installation. # during installation früher im Satz?

?-at your nearest bookstore.
?+at your the bookstore of your choice. # oder closest?

?to pass information securely. # pass=>transfer|transmit?
?passes # dito

known by its command mgr-ssl-tool . This # extra space

?or from a system other than their SUSE Linux Enterprise Server. # passt irgendwie nicht zum Satz davor

?that connects to the central SUSE Manager Servers # wie im vorherige Kapitel - oder gibt es diese Zentralen SUSE Manager Server?

?When adding SUSE Manager Proxy Servers of versions prior to 1.2 into your SUSE Manager infrastructure. # gibt es die überhaupt?

-of an SUSE Manager Proxy Server
+of a SUSE Manager Proxy Server

-to an SUSE Manager Server
+to a SUSE Manager Server

-for each Proxies and Servers
+for each Proxy and Server

?Delete the build tree from the build system for security purposes # purposes=>reasons? wordorder

command line option help listings # Substantivhaufen

?man mgr-ssl-tool # geht das?

This must be issued with any of # must und any passen hier nicht so zusammen # mehrmals

?Record it in a safe manner. # safe & secure?

?Displays verbose messaging # messaging=>messages?

?admin@example.corp # wieso nicht bei example.com bleiben? braucht evtl. code change?

--password=MY_CA_PASSWORD ... # kursive placeholder # mehrmals

/usr/share/rhn/ RHN-ORG-TRUSTED-SSL-CERT # extra space

?Note that the value for --set-hostname is different for each server. # is=>must be? Note=>Take care|Ensure?

?end up in subdirectories of the build directory that reflect the build system's machine name # should reflect the --set-hostname value (which only defaults to build system's hostname) to not overwrite other servers' files

/etc/httpd/conf/ssl.key/server.key # openSUSE hat /etc/apache2/ssl.key/server.key etc - stimmt das im Code?

?Note that the httpd service must be restarted # httpd=>apache2?
 restarting the httpd service afterwards # dito

Once the RPM or raw certificate has been deployed to a client system, the administrator of that system must then # Once...then klingt komisch


spellchecked http://docserv.suse.de/generated/books/Manager/susemanager-client-config_remarks_comments_draft/ch-gpg-keys.html
?The key can then be downloaded by client systems using Wget # Wget=>wget?

spellchecked http://docserv.suse.de/generated/books/Manager/susemanager-client-config_remarks_comments_draft/ch-bootstrap.html

scroipt
boostrap

spellchecked http://docserv.suse.de/generated/books/Manager/susemanager-client-config_remarks_comments_draft/ch-scripting.html
OK

spellchecked http://docserv.suse.de/generated/books/Manager/susemanager-client-config_remarks_comments_draft/ch-rhnreg-ks.html
manager_clientconfig_activ_keys.xml # active?

spellchecked http://docserv.suse.de/generated/books/Manager/susemanager-client-config_remarks_comments_draft/ap-sample-bootstrap-script.html
clinet
explicitely


